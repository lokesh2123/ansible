- namme: error handling
  hosts: localhost
  become: true

  vars_files:
    - variables.yaml
  
  tasks:
  - name: check the user output
    register: user_output
  - name: print
    debug:
      msg: {{ user_output }}

    #command: id roboshop