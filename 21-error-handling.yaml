- name: error handling
  hosts: localhost
  become: yes

  vars_files:
    - variables.yaml
  
  tasks:
  - name: check the id 
    command: id roboshop
    ignore_error: true
    
  - name: create user 
    command: useradd roboshop


